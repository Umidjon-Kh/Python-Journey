from time import perf_counter
from foonctools import wraps

class Time:
    """
    A tool for determinig the time takes to complete provided
    function or task. Simple but timer but have many format
    options to present time.
    """

    def __init__(self, format: OPtional) -> None:
        self.time_format =
